using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Insight.Core.Domain
{
    public enum Operators
    {
        EQUALS = 1,
        NOT_EQUALS = 2,
        GREATER_THAN = 3,
        LESS_THAN = 4,
        IS_NULL = 5,
        NOT_NULL = 6,
        LIKE = 7,
        AND = 8,
        OR = 9,
        BETWEEN = 10
    }
}
